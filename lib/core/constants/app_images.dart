/// Bundled image asset paths — the only place asset strings live.
abstract final class AppImages {
  static const _base = 'assets/images';

  /// Osta mark (square logo).
  static const logo = '$_base/logo.png';

  /// Full lockup (mark + wordmark).
  static const fullLogo = '$_base/full_logo.png';

  /// Mascot artwork.
  static const mascot = '$_base/osta.png';
}
